# Maven demo for Kabisa Proficiency Scales

## Build and run the project

First build the project using Maven

```
mvn package
```

And then run the JAR via:

```
java -jar target/novice-1.0-SNAPSHOT.jar
```